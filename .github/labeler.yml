dependencies:
  - changed-files:
      - "**/go.mod"
      - "**/go.sum"
      - "**/requirements.txt"

docker:
  - changed-files:
      - "docker/**"

github_actions:
  - changed-files:
      - ".github/workflows/*.yml"
      - ".github/workflows/*.yaml"

go:
  - changed-files:
      - "**/*.go"

ai:
  - changed-files:
      - "ai/**"
      - "**/ai_*.go"
